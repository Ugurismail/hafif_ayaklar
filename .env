# Django Configuration
# CRITICAL: Generate a new secret key for production using:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-secret-key-here

# Debug mode - MUST be False in production
# Set to 'False' (as string) in production
DEBUG=True

# Allowed hosts - comma separated list (e.g., yourdomain.com,www.yourdomain.com)
ALLOWED_HOSTS=127.0.0.1,localhost

# CSRF Trusted Origins - comma separated list for production (HTTPS URLs)
# Example: https://yourdomain.com,https://www.yourdomain.com
# This is REQUIRED for production to allow POST requests from your domain
CSRF_TRUSTED_ORIGINS=

# Database Configuration
# For PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://username:password@localhost:5432/hafif_ayaklar
# For SQLite (development only):
# DATABASE_URL=sqlite:///db.sqlite3
DATABASE_URL=

# Email Configuration (optional - for password reset, notifications)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@hafifayaklar.com

# Security Settings (Production Only - uncomment and set to True)
# SESSION_COOKIE_SECURE=False
# CSRF_COOKIE_SECURE=False
# SECURE_SSL_REDIRECT=False
# SECURE_HSTS_SECONDS=0
# SECURE_HSTS_INCLUDE_SUBDOMAINS=False
# SECURE_HSTS_PRELOAD=False

# Cache Configuration (optional - Redis recommended for production)
# REDIS_URL=redis://localhost:6379/0

# Static and Media Files
# STATIC_ROOT=/path/to/static
# MEDIA_ROOT=/path/to/media

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Admin Configuration
# ADMIN_URL_PATH=admin  # Change this to something unique in production

# Agora.io Configuration (for Radio feature)
# Get these from https://console.agora.io/
# 1. Create an account
# 2. Create a new project
# 3. Get App ID and App Certificate
AGORA_APP_ID=3a52d5c1f2ae4a0d8bbe2e72ec07de3e
AGORA_APP_CERTIFICATE=2d5d611b2bcc4b1db3a8e3a2c9bfa532
